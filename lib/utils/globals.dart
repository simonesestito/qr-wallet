class Globals {
  // Some values (even different in type) to constantly reuse accross the app
  static const double borderRadius = 16;
  static const double avatarRadius = 42;
  static const String appVersion = '1.0.0';
}
